<h1><%= t "all_categories" %></h1>

<table class="table">
  <thead>
    <th><%= t "name" %></th>
  </thead>
  <tbody>
    <% @categories.each do |category|%>
      <tr>
        <td><%= category.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate %>
